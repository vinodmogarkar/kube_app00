apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eshop-ingress
  namespace: eshop  
spec:
  defaultBackend:
    service:
      name: reverseproxy
      port:
        number: 80
  rules:
  - host: eshop.local

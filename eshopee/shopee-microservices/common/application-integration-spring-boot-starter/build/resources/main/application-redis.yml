eshopee:
  broker:
    host: redis
    port: 6379
    password:
    timeout: 60000

spring:
  redis:
    host: ${eshopee.broker.host}
    port: ${eshopee.broker.port}
    password: ${eshopee.broker.password}
    timeout: ${eshopee.broker.timeout}
